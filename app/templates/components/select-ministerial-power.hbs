{{#power-select
    selected=selectedMinisterialPower
    options=options
    onChange=(action "onChange")
    as |ministerialPower|}}
  {{ministerialPower.label}} {{#if ministerialPower.isSpecific}}({{ministerialPower.councilsNumber}}){{/if}}
{{/power-select}}
