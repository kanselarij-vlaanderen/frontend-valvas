{{#power-select
    selected=selectedMinisterialPower
    options=options
    onChange=(action "onChange")
    as |ministerialPower|}}
  {{ministerialPower.label}} {{#if ministerialPower.isSpecific}}({{ministerialPower.councilNumber}}){{/if}}
{{/power-select}}
