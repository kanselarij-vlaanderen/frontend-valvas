{{#power-select
    selected=selectedPresentedBy
    options=options
    onChange=(action "onChange")
    as |presentedBy|}}
  {{presentedBy.label}} {{#if presentedBy.councilsNumber}}({{presentedBy.councilsNumber}}){{/if}}
{{/power-select}}
