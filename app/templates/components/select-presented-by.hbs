{{#power-select
    selected=selectedPresentedBy
    options=options
    onChange=(action "onChange")
    as |presentedBy|}}
  {{presentedBy.label}} {{#if presentedBy.isSpecific}}({{presentedBy.councilsNumber}}){{/if}}
{{/power-select}}
