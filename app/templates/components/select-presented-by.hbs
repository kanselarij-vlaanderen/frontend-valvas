{{#power-select
    selected=selectedPresentedBy
    options=options
    onChange=(action "onChange")
    as |presentedBy|}}
  {{presentedBy.label}} {{#if presentedBy.isSpecific}}({{presentedBy.councilNumber}}){{/if}}
{{/power-select}}
