<div class="vl-badge vl-badge--icon vl-badge--icon-light vl-badge--alt vl-badge--small vl-icon-wrapper vl-u-spacer-extended-right-s">
  <i class="vl-badge__icon vi vi-document-small"></i>
</div>
{{#unless fetchRecord.isRunning}}
  {{#if documentVersions}}
    <button class="link" {{action (mut isExpanded) true}}>
      {{#if (eq documentVersions.length 1)}}
        1 document
      {{else}}
        {{documentVersions.length}} documenten
      {{/if}}
    </button>
    {{#if isExpanded}}
      <div class="document-drawer">
        <div class="u-align-right">
          <button class="link link--icon--close" title="Venster sluiten" {{action (mut isExpanded) false}}>
            <span class="u-visually-hidden">Venster sluiten</span>
          </button>
        </div>
        <div class="grid">
          {{#each documentVersions as |documentVersion|}}
            <div class="col--5-12 col--12-12--m vl-u-spacer-extended-bottom">
              <div class="document-miniature">
                <div class="document-miniature__type">
                  <span class="document-miniature__type__text">{{documentVersion.file.extension}}</span>
                </div>
                <div class="document-miniature__content">
                  {{#link-to "document-view" documentVersion.id target="_blank"}}
                    {{documentVersion.file.filenameWithoutExtension}}
                  {{/link-to}}
                  <div class="document-miniature__metadata">{{documentVersion.file.extension}} - {{documentVersion.file.humanReadableSize}}</div>
                </div>
              </div>
            </div>

          {{/each}}
        </div>
      </div>
    {{/if}}
  {{else}}
    <button class="link" {{action (mut isExpanded) true}}>
      Geen documenten beschikbaar
    </button>
    {{#if isExpanded}}
      <div class="filter__form">
        <div class="u-align-right">
          <button class="link link--icon--close" title="Venster sluiten" {{action (mut isExpanded) false}}>
            <span class="u-visually-hidden">Venster sluiten</span>
          </button>
        </div>
        <p>
          Op 29 januari 2016 besliste de Vlaamse Regering om alle nota’s en mededelingen actief openbaar te maken, met uitzondering van de documenten bij individuele beslissingen. Individuele beslissingen regelen een persoonlijke rechtstoestand en gelden voor één geval of voor enkele welbepaalde gevallen, zoals benoemingen, subsidiebesluiten voor projecten of investeringssubsidies voor bedrijven.
        </p>
      </div>
    {{/if}}
  {{/if}}
{{/unless}}